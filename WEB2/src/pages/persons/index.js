const React = require('react')

const ListPersons = React.createClass({
    render() {
        return (
            <div>
                <h1>Listing Persons</h1>
            </div>
        )
    }
})

module.exports = ListPersons

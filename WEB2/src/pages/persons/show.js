const React = require('react')

const ShowPerson = React.createClass({
    render() {
        return (
            <div>
                <h1>SHOWING A PERSON</h1>
            </div>
        )
    }
})

module.exports = ShowPerson
